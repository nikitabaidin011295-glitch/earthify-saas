[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx prisma generate && npx tsc --noEmitOnError false || true"

[deploy]
startCommand = "npx prisma migrate deploy && node dist/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
